@model IEnumerable<Bank.BAL.AccountModel>



@if (Request.IsAuthenticated) { 

<button type="button" class="btn navbar-btn">@Html.ActionLink("Back", "Index", "Home")</button>

<button type="button" class="btn navbar-btn">
    @Html.ActionLink("Create New", "LoginIndex")
</button>
<table class="table table-hover table-bordered">
    <tr>
      
        <th>
            @Html.DisplayNameFor(model => model.User.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Branch.BranchName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AccountNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MinimumBalance)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.WithdrwaLimit)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AccountBalance)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
    <tr>
       
        <td>
            @Html.DisplayFor(modelItem => item.User.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Branch.BranchName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AccountNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MinimumBalance)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WithdrwaLimit)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AccountBalance)
        </td>
        <td>
            <button type="button" class="btn navbar-btn"> 
                 @Html.ActionLink("Edit", "Edit", new { @id = item.AccountId }, new { /* id=item.PrimaryKey */ }) 
            </button>
            <button type="button" class="btn navbar-btn">
                @Html.ActionLink("Details", "GetSingleAccountDetails", new { @id = item.AccountId }, new { /* id=item.PrimaryKey */ }) 
            </button>
            <button type="button" class="btn navbar-btn">  
                @Html.ActionLink("Delete", "Delete", new { @id = item.AccountId }, new { /* id=item.PrimaryKey */ })
            </button>
        </td>
    </tr>
    }

</table>
}