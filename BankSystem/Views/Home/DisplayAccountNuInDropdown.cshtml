@model Bank.BAL.AccountModel

<br /><br />
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("DisplayAccountNuInDropdown", "Home", FormMethod.Get))
    {
        <span><b>Select Account::&nbsp;&nbsp;&nbsp;</b></span>@Html.DropDownListFor(model => model.AccountId, new SelectList(Model.AccountList, "AccountId", "AccountNumber"), "Select Account")

        <input type="submit" value="See Balance" />
        @*<div class="form-group">
                @Html.LabelFor(model => model.User.FirstName, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    <span>@Model.User.FirstName</span><br />
                    @Html.TextBoxFor(model => model.User.FirstName, new { htmlAttributes = new { @class = "form-control" } })

                    @Html.ValidationMessageFor(model => model.User.FirstName, "", new { @class = "text-danger" })
                </div>
            </div>*@


    }

    <button type="button" class="btn navbar-btn">@Html.ActionLink("Deposit", "GetDetailsOfAccount", "Home")</button>
    <button type="button" class="btn navbar-btn">@Html.ActionLink("Withdrawl", "GetDetailsOfAccount", "Home")</button>
}
